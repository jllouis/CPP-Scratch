//
// Created by lael on 1/22/18.
//
#include <bits/stdc++.h>

using namespace std;

string getShortestUniqueSubstring(const vector<char> &arr, const string &str) {
    // your code goes here
    vector<char>::size_type arr_size = arr.size();
    string::size_type str_size = str.size();

    for (vector<char>::size_type width = arr_size; width <= str_size; width++) {
        for ()
    }

    iterator kk = arr.begin();
}

int main() {
    return 0;
}